import { connect } from "react-redux";

const Login = (props) => {
  return (
    <div>
      <h1>Login here</h1>
    </div>
  );
};

const mapStateToProps = ({}) => ({});

export default connect(mapStateToProps)(Login);
